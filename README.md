# jQuery_validation_maxwords
Validate textarea fields for max words. Function will return false if max words are more then required.

Jquery validation plugin https://jqueryvalidation.org/

Uses:
``` 
form.validate({
    field_name: {
        maxwords: 6,
    },
  },
});
```
